# Failover Scenarios with Your Current Configuration\n\n## Your Settings Summary\n\n```yaml\nsynchronous_standby_names: ANY 2 (pg2, pg4)\nsynchronous_mode: true\nsynchronous_mode_strict: false  # ← Allows writes if standby fails\nmaximum_lag_on_failover: 1048576  # ← 1 MB data loss tolerance\n```\n\n---\n\n## Scenario 1: pg1 (Leader) Fails — Healthy State\n\n**Before Failure:**\n```\npg1 (Leader)    ✅ Running\npg2 (Sync SB)   ✅ Running, has all data\npg3 (Replica)   ✅ Running\npg4 (Standby)   ✅ Running, has all data\n```\n\n**What Happens:**\n\n1. Patroni detects pg1 is unreachable\n2. Checks `synchronous_mode: true` → \"Is pg2 fully synced?\"\n3. YES ✅ → Promotes pg2 to leader\n4. pg2 becomes new leader, writes resume\n5. pg3 and pg4 stream from pg2\n\n**Data Loss:** 0 bytes (RPO = 0) ✅\n\n**Recovery Time:** ~10-15 seconds ✅\n\n---\n\n## Scenario 2: pg4 (Region B Standby) Fails\n\n**Before Failure:**\n```\npg1 (Leader)    ✅ Running\npg2 (Sync SB)   ✅ Running, has all data\npg3 (Replica)   ✅ Running\npg4 (Standby)   ❌ FAILS (network down, disk full, etc.)\n```\n\n**What Happens:**\n\n1. pg1 tries to send WAL to pg4, gets no ACK\n2. pg1 has pg2 ACK ✅, but can't get pg4 ACK ❌\n3. Patroni checks `synchronous_mode_strict: false` → Allow writes anyway\n4. pg1 **continues writing with just pg2 as sync** ✅\n5. pg4 remains standby (reconnects when available)\n\n**What You See:**\n```bash\ndocker exec pg1 patronictl list\n# pg1: Leader\n# pg2: Sync Standby  ← Only this is now synchronous\n# pg3: Replica\n# pg4: Standby       ← Marked as \"unhealthy\" in etcd\n```\n\n**Data Loss:** 0 bytes (still have pg2) ✅\n**Write Latency:** Reduced (no longer waiting for pg4)\n\n---\n\n## Scenario 3: pg2 (Sync Standby) Fails — pg1 Still Running\n\n**Before Failure:**\n```\npg1 (Leader)    ✅ Running\npg2 (Sync SB)   ❌ FAILS (power loss)\npg3 (Replica)   ✅ Running\npg4 (Standby)   ✅ Running\n```\n\n**What Happens:**\n\n1. pg1 tries to send WAL to pg2, gets no ACK\n2. pg1 has no pg2 ACK ❌, but pg4 is still there ✅\n3. pg1 has pg4 ACK ✅, but needs ANY 2 of {pg2, pg4}\n4. **pg1 CANNOT get 2 acks** → Write blocks! ⏳\n\n**But wait:** `synchronous_mode_strict: false` means:\n- Patroni **demotes pg2** from sync (it's dead)\n- Patroni **promotes pg3 or pg4** to sync status\n- pg1 **resumes writes** with new sync standby ✅\n\n**Timeline:**\n```\nT=0s:   pg2 fails\nT=0-10s: pg1 write blocks (waiting for 2 acks)\nT=10s:  Patroni detects pg2 is dead, demotes it\nT=10s:  Patroni picks pg3 or pg4 as new sync standby\nT=10s:  pg1 resumes writes (now only needs new sync) ✅\n```\n\n**Data Loss:** 0 bytes (pg4 has the data) ✅\n**Downtime:** ~10 seconds for failover detection + promotion\n\n---\n\n## Scenario 4: Both pg1 and pg2 Fail (Partial Cluster Loss)\n\n**Before Failure:**\n```\npg1 (Leader)    ❌ FAILS\npg2 (Sync SB)   ❌ FAILS\npg3 (Replica)   ✅ Running\npg4 (Standby)   ✅ Running\n```\n\n**What Happens:**\n\n1. Patroni checks: \"Is pg3 or pg4 fully synced with pg1+pg2?\"\n2. `synchronous_mode: true` requires strict sync check\n3. **Problem:** pg3 and pg4 may not have latest transactions from pg1\n4. `maximum_lag_on_failover: 1048576` (1 MB) allows up to 1 MB loss\n5. **If pg3/pg4 are within 1 MB:** Promote pg3 or pg4 as leader\n\n**Data Loss:** Up to 1 MB (1048576 bytes) ⚠️\n**Why?** pg1+pg2 failed before pg3/pg4 caught up\n\n---\n\n## Scenario 5: Multi-Region Failover (pg1+pg2+pg3 All Fail)\n\n**Before Failure:**\n```\nRegion A:\npg1 (Leader)    ❌ FAILS (entire datacenter down)\npg2 (Sync SB)   ❌ FAILS\npg3 (Replica)   ❌ FAILS\n\nRegion B:\npg4 (Standby)   ✅ Running (has all committed data due to sync mode)\n```\n\n**What Happens:**\n\n1. Entire Region A is down\n2. pg4 is the only node alive\n3. Patroni on pg4 detects Region A is unreachable\n4. pg4 has `standby_cluster` config pointing to Region A (172.18.0.10)\n5. pg4 cannot connect to leader anymore\n6. **Automated failover:** Does NOT happen (Patroni is conservative)\n\n**Manual Intervention Required:**\n\n```bash\n# On pg4, manually promote to standalone:\ndocker exec pg4 patronictl -c /etc/patroni/patroni.yml switchover \\\n  --force --targetname pg4\n\n# Or use:\ndocker exec pg4 patronictl -c /etc/patroni/patroni.yml failover\n```\n\n**After Manual Promotion:**\n```\nRegion B:\npg4 (Standby → now Leader) ✅ Running\n```\n\n**Data Loss:** 0 bytes ✅ (pg4 had all sync data)\n**Recovery Time:** ~5 minutes (manual promotion)\n\n---\n\n## Summary: When You Lose Data\n\n| Scenario | Data Loss | Reason | Prevention |\n|----------|-----------|--------|-------------|\n| Only pg1 fails | 0 bytes | pg2 has it | Sync mode ✅ |\n| pg4 fails | 0 bytes | pg2 has it | Sync mode ✅ |\n| pg2 fails | 0 bytes | pg4 has it | Async replication ✅ |\n| pg1+pg2 fail, pg3/pg4 far behind | ~1 MB | max_lag_on_failover | Use slot for pg4 |\n| All Region A fails, pg4 sync | 0 bytes | pg4 got everything | Cross-region sync ✅ |\n\n---\n\n## Recommendation\n\n**To achieve RPO = 0 across all scenarios:**\n\n1. Set `maximum_lag_on_failover: 0` (no tolerance for lag)\n2. Use physical replication slot for pg4 (keeps it in sync without write latency)\n3. Monitor pg4 replication lag regularly\n\n```bash\ndocker exec pg1 patronictl -c /etc/patroni/patroni.yml edit-config pg-docker-cls1 \\\n  --force --set maximum_lag_on_failover=0\n```\n"
