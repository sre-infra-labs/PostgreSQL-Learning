# Your Synchronous Replication Configuration Explained\n\n## Current Settings from `patronictl show-config`\n\n```\nsynchronous_standby_names: ANY 2 (pg2, pg4)\nsynchronous_mode: true\nsynchronous_mode_strict: false\nsynchronous_node_count: 1\nmaximum_lag_on_failover: 1048576\n```\n\n---\n\n## Setting 1: `synchronous_standby_names: ANY 2 (pg2, pg4)`\n\n### What it means:\n\n**When pg1 (leader) writes data, it waits for acknowledgment from ANY 2 of these replicas:**\n- pg2 (Sync Standby in Region A)\n- pg4 (Standby in Region B)\n\n### How it works:\n\n```\npg1 (Leader) writes WAL\n    ↓\n    ├→ pg2 (Region A) — ACK ✅ (5-10ms)\n    ├→ pg4 (Region B) — ACK ✅ (50-200ms cross-region)\n    ↓\npg1: \"I got 2 acks! Write is SAFE. Return SUCCESS to client\"\n```\n\n**Transaction commits only when:**\n- ✅ pg2 acknowledges (local, fast)\n- ✅ AND pg4 acknowledges (cross-region, slow)\n\n### Cost:\n\n- ⚠️ **Write latency: +50-200ms** (waiting for pg4 across regions)\n- ⚠️ **If pg4 is unreachable → primary becomes READ-ONLY** (can't get 2 acks)\n- ⚠️ **If pg4 is slow → all writes slow down**\n\n### RPO (Recovery Point Objective):\n\n- **RPO = 0** (Zero data loss) — both pg2 and pg4 have the data before commit\n- Safe for critical data that cannot be lost\n\n---\n\n## Setting 2: `synchronous_mode: true`\n\n### What it means:\n\n**Patroni enforces strict synchronous replication rules:**\n\n1. **Will NOT promote a standby unless it has all transactions** the primary committed\n2. **Primary may go read-only** if no synchronous standby available\n3. **Patroni manages which replicas are \"sync\"** (can be promoted safely)\n\n### Behavior:\n\n```\nScenario: pg1 fails, pg2 is far behind\n\nWithout synchronous_mode (async):\n  → Patroni promotes pg2 anyway\n  → Some committed transactions are lost ❌\n\nWith synchronous_mode (sync):\n  → Patroni checks: \"Does pg2 have all transactions?\"\n  → If NO: Primary remains down until pg2 catches up\n  → If YES: Safe to promote pg2 ✅\n```\n\n### Impact:\n\n- ✅ **Prevents unexpected data loss**\n- ⚠️ **Cluster may be unavailable for writes** if no sync standby available\n- ✅ **Patroni tracks sync state** automatically\n\n---\n\n## Setting 3: `synchronous_mode_strict: false`\n\n### What it means:\n\n**Patroni allows the primary to continue writes EVEN IF synchronous standby is unavailable:**\n\n```\nWith synchronous_mode_strict: false (current):\n  → If pg4 becomes unreachable\n  → Patroni demotes pg4 from \"sync\" status\n  → pg1 continues to write with just pg2 as sync ✅\n  → RPO still protected (pg2 has everything)\n\nWith synchronous_mode_strict: true (stricter):\n  → If pg4 becomes unreachable\n  → pg1 STOPS accepting writes ❌\n  → Cluster is read-only until pg4 comes back\n```\n\n### Your Setup:\n\n**You have `synchronous_mode_strict: false` which means:**\n- ✅ Primary keeps running even if pg4 fails\n- ✅ Patroni automatically adjusts sync standby list\n- ✅ Balanced approach (safety + availability)\n\n---\n\n## Setting 4: `synchronous_node_count: 1`\n\n### What it means:\n\n**Patroni must maintain AT LEAST 1 synchronous standby at all times:**\n\n```\nYour cluster has 3 replicas: pg2, pg3, pg4\n\nPatroni picks: pg2 is synchronous (sync_state = 'sync')\nOthers (pg3, pg4): are \"async\" (sync_state = 'async')\n\nIf pg2 fails:\n  → Patroni promotes one of {pg3, pg4} to sync_state = 'sync'\n  → Maintains synchronous_node_count = 1\n```\n\n### Why 1 instead of 2?\n\n- **Count: 1** = Only 1 replica needs to ack (default, balanced)\n- **Count: 2** = 2 replicas must ack (stricter RPO=0 but higher latency)\n- **Count: 3** = All 3 must ack (very strict, likely overkill)\n\n**Your setup uses count:1** which means:\n- Only pg2 MUST be in sync state\n- pg3 and pg4 are \"optional\" for sync\n- pg1 waits for ANY 2 of {pg2, pg4} (configured separately)\n\n---\n\n## Setting 5: `maximum_lag_on_failover: 1048576`\n\n### What it means:\n\n**If primary crashes, Patroni allows promoting a standby that is UP TO 1048576 bytes behind:**\n\n```\n1048576 bytes = 1 MB of WAL\n\nScenario: pg1 crashes with uncommitted WAL\n\nWithout this setting (default 0):\n  → Only promote standby with EXACT same LSN ✅\n  → More data safe but might not have healthy standby\n\nWith this setting (1 MB):\n  → Promote if standby is within 1 MB\n  → Allows data loss UP TO 1 MB ⚠️\n  → More likely to have healthy standby ✅\n```\n\n### Your Configuration:\n\n**1048576 bytes (1 MB) means:**\n- Acceptable data loss: ~1 MB of WAL = ~5000-10000 transactions\n- Failover is faster (doesn't wait for perfect sync)\n- Trade-off: RPO ~1 MB ⚠️\n\n⚠️ **WARNING**: This contradicts your `synchronous_mode: true` (which should be RPO=0)\n\n---\n\n## Configuration Consistency Check ⚠️\n\n**You have conflicting settings:**\n\n| Setting | Value | RPO | Purpose |\n|---------|----|-----|---------|\n| `synchronous_mode: true` | ON | 0 sec | Strict sync |\n| `synchronous_mode_strict: false` | OFF | — | Allow writes if standby fails |\n| `synchronous_standby_names` | ANY 2 (pg2, pg4) | 0 sec | Require 2 acks |\n| `maximum_lag_on_failover` | 1 MB | 1 MB | Allow up to 1 MB loss |\n\n**Resolution**: `maximum_lag_on_failover` will allow up to 1 MB data loss even in sync mode.\n\n**Recommendation**: Set to 0 if you want strict RPO=0:\n```bash\ndocker exec pg1 patronictl -c /etc/patroni/patroni.yml edit-config pg-docker-cls1 \\\n  --force --set maximum_lag_on_failover=0\n```\n\n---\n\n## Summary Table\n\n| Parameter | Current | Effect |\n|-----------|---------|--------|\n| `synchronous_standby_names` | ANY 2 (pg2, pg4) | Writes wait for 2 acks; RPO=0 |\n| `synchronous_mode` | true | Won't promote standby without all data |\n| `synchronous_mode_strict` | false | Primary keeps running if standby fails |\n| `synchronous_node_count` | 1 | At least 1 replica must be sync |\n| `maximum_lag_on_failover` | 1 MB | Allow up to 1 MB data loss (⚠️ conflict) |\n\n**Your setup**: Mostly RPO=0 (zero data loss) with 50-200ms write latency for cross-region DR safety. ✅\n"
