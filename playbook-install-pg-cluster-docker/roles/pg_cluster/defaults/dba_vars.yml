# REFERENCE ONLY — not loaded at runtime.
#
# These variables are provided by vars/dba_vars.yml (loaded via vars_files
# in the calling playbook). Fallback defaults live in defaults/main.yml.
#
# Keep this file in sync with vars/dba_vars.yml for documentation purposes.

# patroni_cluster_name: "pg-docker-cls1"
# pgbackrest_stanza_name: "pg-docker-cls1"
# postgresql_version: "18"
# postgresql_port: "5432"
# server_hardware:
#   cpu_count: 2
#   ram_gb: 4
#   disk_gb: 20
# pg_params:
#   work_mem: "4MB"
#   maintenance_work_mem: "256MB"
# db_name: "dba"
# db_user_rw: dba_rw
# db_user_ro: dba_ro
# docker_network_name: "lab-network"
# pgbouncer_listen_port: 6432
# pgbouncer_pool_mode: "transaction"
# pgbouncer_max_client_conn: 200
# pgbouncer_default_pool_size: 25
