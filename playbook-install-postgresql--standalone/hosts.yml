all:
  children:
    debian:
      hosts:
        # pg-pub:
        # pg-sub:
        # pgpoc:
        # pgpractice:
        sqlbuntu:
    redhat:
      hosts:
        # sqlred:
        # pg-pub2:
        # pg-sub2: